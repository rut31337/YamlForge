guid: "demo1"

yamlforge:
  cloud_workspace:
    name: "simple-demo-{guid}"
    description: "Simple single VM demonstration"
  
  instances:
    - name: "simple-vm-{guid}"
      provider: "aws"
      flavor: "small"
      image: "RHEL9-latest"
      region: "us-east-1" 
